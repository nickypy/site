{{ define "body" }}

{{ $length := len .BlogItems }}
{{ if ne $length 0 }}
<h2>blog</h2>
<ul class="home-ul">
  {{ range .BlogItems }}
    <li>
      <span class="home-li">
        <p class="mono">{{ .FormatDate }}</p>
        <a href="{{.Path}}">{{.Metadata.Title}}</a>
      </span>
    </li>
  {{ end }}
</ul>
{{ end }}

{{ $length := len .Links.External }}
{{ if ne $length 0 }}
<h2>publications</h2>
<ul class="home-ul">
  {{ range .Links.External }}
  <li>
    <span class="home-li">
      <p class="mono">{{ .FormatDate }}</p>
      <a href="{{ .Link }}" target="_blank">{{ .Title }}</a>
    </span>
  </li>
  {{ end }}
</ul>
{{ end }}

{{ end }}
